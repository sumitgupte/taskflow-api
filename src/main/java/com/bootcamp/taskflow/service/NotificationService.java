package com.bootcamp.taskflow.service;

/**
 * Placeholder for outbound notifications (reminders, digests, alerts). Nothing is wired up yet -
 * there is no scheduler, no transport, and no persistence behind this class. Intentionally not
 * annotated with {@code @Service} until it has real behavior.
 */
public class NotificationService {}
