package com.bootcamp.taskflow.service;

// Placeholder class. There is currently no reminder / notification system
// for tasks approaching their dueDate. This class exists so the lab ticket
// (see docs/lab-ticket-1-refine.md) has an obvious place to build the
// feature into, without dictating the implementation.
//
// Nothing here is real yet - it's just a marker for where the feature will
// live. Intentionally not annotated as a @Service until the lab adds real
// behavior to it.
public class NotificationService {}
