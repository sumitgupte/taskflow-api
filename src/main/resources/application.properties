server.port=3000
spring.application.name=taskflow-api

# Demo-only secret. Overridable via TASKFLOW_JWT_SECRET env var.
# Do not reuse this value outside the lab environment.
taskflow.jwt.secret=${TASKFLOW_JWT_SECRET:bootcamp-demo-secret-please-change-32bytes}
taskflow.jwt.expiry-minutes=120
